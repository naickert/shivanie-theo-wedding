# Dependencies (only used for local Playwright testing)
node_modules/
package.json
package-lock.json

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Local server output
*.log

# Local-only test artefacts
tools/verify-admin.mjs

# Couple-only consoles — used locally only, never deployed. The live site is
# PUBLIC, and these pages carry the admin gate and can render guest data when
# run beside the plaintext guest list.
admin/
tools/generate-codes.html
tools/preview-link.html

# Private guest list — the deployed site is PUBLIC, so plaintext must never be
# committed. Only the encrypted data/guests.enc.json ships. After editing this
# file, regenerate the encrypted version with: node tools/encrypt-guests.mjs
data/guests.json
